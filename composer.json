{
    "name": "silaswint/laravel-mailcatcher",
    "require": {
        "spatie/laravel-package-tools": "^1.16",
        "illuminate/support": "^10 || ^11 || ^12",
        "illuminate/mail": "^10 || ^11 || ^12",
        "illuminate/config": "^10 || ^11 || ^12",
        "laravel/framework": "^10 || ^11 || ^12",
        "aws/aws-sdk-php": "^3.295.10"
    },
    "autoload": {
        "psr-4": {
            "SilasWinter\\MailCatcher\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "SilasWinter\\MailCatcher\\MailCatcherServiceProvider"
            ]
        },
        "aws/aws-sdk-php": [
            "Ses"
        ]
    }
}
